# ==================================
# Anonyma Full Stack Configuration
# ==================================

# PostgreSQL Database
POSTGRES_DB=anonyma
POSTGRES_USER=anonyma
POSTGRES_PASSWORD=change_this_password_in_production

# Database URL for API
DATABASE_URL=postgresql://anonyma:change_this_password_in_production@postgres:5432/anonyma

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# JWT Authentication
JWT_SECRET=change_this_jwt_secret_generate_a_random_string
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# API Configuration
ANONYMA_APP_NAME=Anonyma API
ANONYMA_DEBUG=false
ANONYMA_AUTH_ENABLED=true
ANONYMA_REDIS_ENABLED=true
ANONYMA_RATE_LIMIT_ENABLED=true
ANONYMA_RATE_LIMIT_REQUESTS=100
ANONYMA_RATE_LIMIT_WINDOW=60

# Demo Mode Configuration
DEMO_MODE_ENABLED=true
DEMO_DAILY_LIMIT=50
DEMO_MONTHLY_LIMIT=500

# Premium User Limits
PREMIUM_DAILY_LIMIT=1000
PREMIUM_MONTHLY_LIMIT=10000

# Admin has unlimited access (999999)

# Frontend Configuration
REACT_APP_API_URL=http://localhost

# Ports
API_PORT=8000
FRONTEND_PORT=3000
NGINX_PORT=80

# Default Admin Credentials (CHANGE THESE!)
DEFAULT_ADMIN_EMAIL=admin@anonyma.local
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123

# ==================================
# Instructions
# ==================================

# 1. Copy this file to .env:
#    cp .env.example .env

# 2. Generate a secure JWT secret:
#    openssl rand -hex 32

# 3. Change all passwords to secure values

# 4. Start the stack:
#    docker-compose -f docker-compose.full.yml up -d

# 5. Access the application:
#    http://localhost (main app)
#    http://localhost:8000/docs (API docs)

# ==================================
# Security Notes
# ==================================

# - NEVER use default passwords in production
# - ALWAYS use HTTPS in production
# - Generate strong random secrets
# - Keep this .env file secure and never commit it
# - Rotate JWT secrets periodically
